<div class="container" style="padding: 60px 0;">
  <div class="section-header">
    <h1 class="section-title">InstaViewer Blog</h1>
    <p class="section-subtitle">Tips, guides, and news about anonymous Instagram viewing</p>
  </div>

  <div style="max-width: 800px; margin: 0 auto;">
    <% articles.forEach(article => { %>
    <a href="/blog/<%= article.slug %>" style="text-decoration: none; color: inherit;">
      <div class="feature-card" style="margin-bottom: 20px; cursor: pointer;">
        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
          <span style="font-size: 0.8rem; color: var(--text-muted);"><%= article.date %></span>
          <span style="font-size: 0.8rem; color: var(--accent-pink);"><%= article.readTime %></span>
        </div>
        <h2 style="font-size: 1.3rem; margin-bottom: 8px;"><%= article.title %></h2>
        <p style="color: var(--text-secondary); margin-bottom: 12px;"><%= article.description %></p>
        <span style="color: var(--accent-pink); font-weight: 600; font-size: 0.9rem;">Read More →</span>
      </div>
    </a>
    <% }) %>

    <div style="text-align: center; margin-top: 40px;">
      <a href="/" class="btn btn-primary">🔍 Try InstaViewer</a>
      <a href="/instagram-story-viewer" class="btn btn-outline">📸 Story Viewer</a>
    </div>
  </div>
</div>
